// Uncomment the following resource block to enable saving state to GCS
// Remember to replace the Bucket ID and other attributes as needed!

/*
terraform {
  backend "gcs" {
    bucket = "<REPLACE_WITH_YOUR_BUCKET_NAME>"
    prefix = "terraform/state"
  }
}
*/

